@model IEnumerable<BibliotecaMetrópolis.Editorial>

@{
    ViewBag.Title = "Index";
}

<h2>Editoriales</h2>

<p>
    @Html.ActionLink("Registrar Editorial", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.IdEdit }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.IdEdit }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.IdEdit })
        </td>
    </tr>
}

</table>
